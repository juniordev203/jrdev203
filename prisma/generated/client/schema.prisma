generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "sqlite"
}

model Reaction {
  id    String @id @default("singleton")
  happy Int    @default(0)
  love  Int    @default(0)
  sad   Int    @default(0)
}
